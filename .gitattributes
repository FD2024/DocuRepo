# Normalize text files
* text=auto eol=lf

# Treat shell scripts as text
*.sh text eol=lf

# Keep binary files untouched
*.jar binary
*.png binary
*.jpg binary
*.pdf binary
*.zip binary

tools/doc/linux/download/* filter=lfs diff=lfs merge=lfs -text
